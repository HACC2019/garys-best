<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hecorewardsactivity">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".homePage"></activity>
        <activity android:name=".creditCard" />
        <activity android:name=".portQues" />
        <activity android:name=".paymentQues" />
        <activity android:name=".teslaQues" />
        <activity android:name=".endPoint" />
        <activity android:name=".successfulPayment" />
        <activity android:name=".chargingSession" />
        <activity android:name=".plate" />
        <activity
            android:name=".splash"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>